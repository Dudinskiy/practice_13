package org.example.controllers;

import org.example.view.View;

public class AddEmployeeController extends Controller{
    public AddEmployeeController(View view, int actionToPerform) {
        super(view, actionToPerform);
    }
}
